package constants;

import java.io.File;

public class Constants {

    public static final String ROOT = "." + File.separator + "src" + File.separator + "test" + File.separator;
    public static final String RESOURCES = ROOT + "resources" + File.separator;
    public static final String SCHEMA_FOLDER = RESOURCES + File.separator + "json_schema" + File.separator;
}
