package constants;

import java.io.File;

public class Constants {

    public static final String RESOURCES = "." + File.separator + "src" + File.separator
            + "test" + File.separator + "resources" + File.separator;
    public static final String SCHEMA_FOLDER = RESOURCES + File.separator + "json_schema" + File.separator;
    public static final String SAMPLES_FOLDER = RESOURCES + File.separator + "request_samples" + File.separator;
}
